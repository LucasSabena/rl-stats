{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "prompt",
  "description": "Capabilities for the focus-taking post-match prompt window",
  "windows": ["prompt"],
  "permissions": [
    "core:event:default",
    "core:window:default"
  ]
}
